#include "1main.h"
/**
 * mod - Computes the remainder of the division of two integers
 * @x: Numerator
 * @z: Denominator
 *
 * Return: Remainder of the division of x by z
 */
int mod(int x, int z)
{
	return (x % z);
}
