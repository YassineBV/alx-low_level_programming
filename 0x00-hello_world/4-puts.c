#include <stdio.h>
#include <string.h>

/**
 * main is the entry point of my program
 * return success = 0
 */ 

int main(void)
{
	char first[] = "\"Programming is like building a multilingual puzzle";

	puts(first);

        return (0);
}
